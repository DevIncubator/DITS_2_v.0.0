<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:beans="http://www.springframework.org/schema/beans"
         xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd">

    <http pattern="/index.jsp" security="none"/>

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/create-user" access="hasRole('ROLE_ADMIN')"/>

        <form-login
                login-processing-url="/login"
                login-page="/index.jsp"
                default-target-url="/admin-home"
                authentication-failure-url="/index.jsp" />

        <logout logout-success-url="/index.jsp"/>

        <csrf disabled="true"/>
    </http>

    <b:bean id="customUserDetailService" class="com.devincubator.service.CustomUserDetailService">
        <b:constructor-arg name="userRepository" ref="userRepository"/>
    </b:bean>

    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailService">
            <password-encoder ref="encoder"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="encoder"
                class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="12"/>
    </beans:bean>

</b:beans>